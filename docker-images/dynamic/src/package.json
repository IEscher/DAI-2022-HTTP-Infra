{
  "name": "config_generator",
  "version": "1.0.0",
  "description": "Generate server's config in a JSON format",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "kbp",
  "license": "ISC",
  "dependencies": {
    "chance": "^1.1.9",
    "docker-names": "^1.2.1",
    "dockerode": "^3.3.4",
    "express": "^4.18.2",
    "node-docker-api": "^1.1.22"
  }
}
